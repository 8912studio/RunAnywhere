<CommandToolbar FixedHeight="24" CrossAxisAlignment="Center">

    <ToolbarButton Name="executeButton" Margin="10,0,0,0" />

    <ToolbarButton Name="preserveButton" Margin="10,0,0,0" />

    <ToolbarButton 
        Name="closeButton"
        Margin="10,0,0,0"
        ImageName="close" 
        Tooltip="Remove from preservation (SHIFT+BACKSPACE)" 
        IsVisible="false" />
    
</CommandToolbar>